<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elevenzon.sqlite">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShiftparPeriodMain"></activity>
        <activity android:name=".shiftparweek" />
        <activity android:name=".shiftperdaymain" />
        <activity android:name=".AllStaffMain" />
        <activity android:name=".staffMain" />
        <activity android:name=".ContoireMain" />
        <activity android:name=".totalperiod" />
        <activity android:name=".totalparjour" />
        <activity android:name=".stockmain" />
        <activity android:name=".notemain" />
        <activity android:name=".chichaMain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>